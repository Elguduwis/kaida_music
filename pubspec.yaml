name: kaida_music
description: A music streaming app built via Termux + GitHub Actions
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  audioplayers: ^5.2.1
  assets_audio_player: ^3.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/music/
